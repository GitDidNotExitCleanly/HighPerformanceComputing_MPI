Edges: 
folder that contains the edge files

Reconstructed_Images:
folder that contains the reconstructed images

Serial:
folder that contains the serial code (for verifying the correctness of parallel program)

Parallel:
main source code folder

* Parallel and Serial both have Makefile inside
* Modify "FILE_NAME","OUTPUT_FILE_NAME","M" and "N" in the macro if you want to use other edge files